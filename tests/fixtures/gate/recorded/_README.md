# Recorded Gate CrossEx fixtures

`live-api/*.json` hold the CAMELCASE `data` payloads returned by the app’s `/api` routes (recorded by tests/live — the raw snake_case wire format is not visible through the server).
